NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional — canonical app URL for join links (defaults to current browser origin)
# NEXT_PUBLIC_APP_URL=https://diagnostic-dash.vercel.app

# AI case generation — Google Gemini (free at aistudio.google.com/apikey)
GEMINI_API_KEY=your-gemini-api-key
# GEMINI_MODEL=gemini-2.5-flash

# Optional — OpenAI (requires billing; school ChatGPT accounts don't include API access)
# OPENAI_API_KEY=sk-your-openai-api-key
# AI_PROVIDER=openai
